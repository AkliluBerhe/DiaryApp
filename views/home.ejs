<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p>
        <%= startingContent %>
    </p>
    <% console.log(postsContent); %>

        <% postsContent.forEach(function(postElement) { %>
            <% console.log(postElement.title); %>
                <h1>
                    <%= postElement.title %>
                </h1>
                <p>
                    <%= postElement.content.substring(0, 100) + " ..." %>
                    <a href="/posts/<%= postElement.title %>">Read More</a>
                </p>
                <% }); %>

                    <!-- <% for(var i=0; i<postsContent.length; i++) { %>
   <% console.log(postsContent[i].title); %>
<% }; %> -->





                    <%- include("partials/footer"); -%>